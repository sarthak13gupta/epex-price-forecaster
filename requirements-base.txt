# ==============================================================================
# BASE — shared by every service. ALL versions are pinned here and nowhere else.
#
# Per-service files add packages but never re-pin one of these, so no two images
# can ever resolve a different version of the same library. That matters because
# the model artifact is a cloudpickle of live objects: a pandas or xgboost
# mismatch between the training image and the serving image breaks unpickling.
# ==============================================================================

# Data and numerics
pandas==2.3.3
numpy==2.5.2
pyarrow==25.0.1
scipy==1.18.1

# Modelling — xgboost is needed at SERVE time too, to unpickle the bundle
scikit-learn==1.9.0
xgboost==3.4.1

# Domain features
holidays==0.103

# Data contracts
pandera==0.32.1

# Configuration and secrets
PyYAML==6.0.3
python-dotenv==1.2.3

# Object storage
boto3==1.43.77

# Model loading and the registry client
mlflow==3.15.1
